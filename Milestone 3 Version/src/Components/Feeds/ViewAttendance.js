import React from 'react';
import { ScrollView } from 'react-native-gesture-handler';
//import {PieChart} from 'react-native-chart-kit';

export default class ViewAttendance {
    render() {
        return(
            <ScrollView>
                <View>
                    
                </View>
            </ScrollView>
        )
    }
}